{
  "name": "todomvc-testing",
  "version": "1.0.0",
  "description": "Testing TodoMVC using Cypress",
  "scripts": {
    "start": "json-server --static . data.json -m middleware.js --config json-server.json",
    "cypress:open": "cypress open"
  },
  "devDependencies": {
    "cypress": "^13.6.1",
    "json-server": "^0.17.4",
    "sendmail": "^1.6.1",
    "typescript": "^5.3.3"
  }
}
